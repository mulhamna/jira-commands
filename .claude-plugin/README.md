# Claude marketplace metadata

This directory stores Claude-facing marketplace metadata for the `jirac` plugin.

## Files

- `marketplace.json` — marketplace manifest for the Claude plugin surface

## Notes

- The Claude Code plugin source of truth lives under `plugin/`.
- ClawHub packaging is handled separately under `clawhub/jirac-plugin/`.
